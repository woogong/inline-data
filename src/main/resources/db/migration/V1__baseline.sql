-- =====================================================================
-- Flyway baseline (V1)
-- =====================================================================
-- 이 파일은 의도적으로 비어있는 baseline 플레이스홀더입니다.
--
-- - 기존 운영 DB: 이미 스키마가 존재하므로 application.yaml의
--   `spring.flyway.baseline-on-migrate=true, baseline-version=1` 설정에 의해
--   이 V1은 적용되지 않고 "baseline으로 간주"됩니다. Flyway는 V2부터 적용합니다.
--
-- - 신규 환경(빈 DB): Flyway가 V1부터 적용하지만 이 파일이 비어있어
--   아무 일도 하지 않습니다. 이후 Hibernate의 `ddl-auto: validate`가
--   테이블 부재로 실패합니다. 신규 환경에서는 최초 1회 `ddl-auto: create`
--   또는 `create-drop`으로 기동해 스키마를 만든 뒤 validate로 되돌리세요.
--   (또는 후속 작업으로 V1에 전체 DDL을 추출해 넣을 수 있습니다.)
-- =====================================================================

SELECT 1;   -- no-op. Flyway는 빈 마이그레이션을 허용하지 않기 때문