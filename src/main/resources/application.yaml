spring:
  application:
    name: inlinedata
  datasource:
    url: jdbc:mariadb://localhost:3306/inlinedata
    username: root
    password:
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
